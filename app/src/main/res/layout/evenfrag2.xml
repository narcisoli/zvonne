<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:background="@color/fundal"
    android:layout_height="match_parent">
<RelativeLayout
    android:layout_width="match_parent"
    android:layout_alignParentBottom="true"
   android:id="@+id/trimitemesaj"
    android:layout_marginTop="10dp"
android:layout_marginBottom="10dp"
    android:layout_height="wrap_content">
<EditText
    android:layout_width="match_parent"
    android:layout_height="40dp"
    android:id="@+id/edittext1"
    android:paddingLeft="5dp"
    android:textSize="@dimen/normal"
    android:background="@drawable/roundedalb"
    android:layout_marginLeft="10dp"
    android:layout_marginRight="45dp"/>
    <ImageView
        android:layout_width="30dp"
        android:layout_height="40dp"
        android:layout_centerVertical="true"
        android:src="@drawable/send"
        android:id="@+id/imaginetrimite1"
       android:layout_alignParentRight="true"
        android:layout_marginLeft="5dp"
        android:layout_marginRight="10dp"
        />
</RelativeLayout>
    <ListView
        android:layout_width="match_parent"
        android:layout_above="@id/trimitemesaj"
        android:divider="@color/material_blue_grey_95"
        android:dividerHeight="0.1dp"
        android:id="@+id/listamesjae1"
        android:layout_height="match_parent">
    </ListView>
</RelativeLayout>